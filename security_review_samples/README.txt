Intentional insecure patterns for validating Cursor Security Review + Snyk Code in CI.
Not imported by the app. Excluded from Docker build via .dockerignore.

The Python module now includes amplified SSRF (urllib, requests, aiohttp, http.client,
ftplib, boto3 endpoint_url), RCE (os.system/popen, subprocess shell, eval/exec/compile),
pickle/marshal deserialization, and SQLite string-interpolation SQLi.

Snyk: CI may use --severity-threshold=high on code test; check the Snyk Code job log
and the Snyk UI for Medium/Low. Delete this folder after the demo if desired.
