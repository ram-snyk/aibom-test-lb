Intentional insecure patterns for validating Cursor Security Review + Snyk Code in CI.
Not imported by the app. Excluded from Docker build via .dockerignore.

Snyk: CI uses --severity-threshold=high on code test; still check the Snyk Code job log
and the Snyk UI for Medium/Low (e.g. SSRF on requests.get). Delete this folder after the demo if desired.
